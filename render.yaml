services:
  - type: web
    name: bfhl-api
    env: node
    rootDir: .
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: 5000
